# FLUX Upscaler Requirements
# Python 3.10+ required (tested with Python 3.10.11)
# For RTX 5060 Ti (Blackwell sm_120) with CUDA support
#
# INSTALLATION INSTRUCTIONS:
# 1. Activate your venv:
#    Windows: .\venv\Scripts\activate
#    Linux/Mac: source venv/bin/activate
#
# 2. Install PyTorch NIGHTLY with CUDA 12.8 FIRST (required for RTX 50-series sm_120):
#    pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
#
# 3. Then install other requirements:
#    pip install -r requirements.txt
#
# NOTE: Do NOT install xformers - it causes PyTorch version conflicts

# Diffusion and AI/ML libraries
diffusers>=0.30.0
accelerate>=1.0.0
einops>=0.8.0
safetensors>=0.4.0

# Image and video processing
Pillow>=10.0.0
opencv-python>=4.8.0
imageio>=2.31.0
imageio-ffmpeg>=0.5.0
av>=12.0.0

# Model loading and utilities
huggingface-hub>=0.20.0
sentencepiece>=0.1.99
spandrel>=0.3.0
gguf>=0.1.0

# ComfyUI dependencies (core)
numpy>=1.24.0
psutil>=5.9.0
requests>=2.31.0
aiohttp>=3.9.0
pyyaml>=6.0
tqdm>=4.66.0

# Additional utilities
torchsde>=0.2.6
omegaconf>=2.3.0

# Development/Notebook tools
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.29.0

# Optional but recommended
tensorboard>=2.15.0
transformers>=4.40.0
